name: Secret scan

# Runs on every PR and push. Fails the build if anything that looks like a
# real production secret is committed. The patterns below cover the formats
# we know are in use (Supabase JWTs, Stripe live keys, GHL Private Integration
# Tokens). Add new patterns here when adopting new vendors.
#
# Note: this workflow does NOT use any untrusted input from issues/PR titles/
# commit messages — all `run:` blocks are static grep over the checked-out tree.

on:
  pull_request:
  push:
    branches: [main, staging]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Scan for hardcoded secrets
        run: |
          set -euo pipefail
          MATCHES=$(grep -rEn \
            --include='*.ts' --include='*.tsx' --include='*.js' --include='*.mjs' \
            --include='*.json' --include='*.sql' --include='*.md' --include='*.yml' \
            --include='*.yaml' \
            --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git \
            --exclude-dir=dist --exclude-dir=build \
            -e 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9\.[A-Za-z0-9._-]{50,}' \
            -e 'sk_live_[A-Za-z0-9]{16,}' \
            -e 'pit-[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}' \
            -e 'whsec_[A-Za-z0-9]{32,}' \
            . || true)

          if [ -n "$MATCHES" ]; then
            echo 'Hardcoded secrets detected:'
            echo "$MATCHES"
            echo ''
            echo 'Replace with environment variables. See README "How to add a new env var".'
            exit 1
          fi
          echo 'No hardcoded secrets found.'
